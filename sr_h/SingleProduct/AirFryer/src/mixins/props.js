import { SPACE_RGE } from '../common/common.js';

export default ( 'status recipes temperature stirSwitch preheatSwitch ' +
                    'workTime leftTime preTimeHour preTimeMin code' ).split( SPACE_RGE );
